<body class="dark:bg-gray-700 dark:text-gray-50 transition-colors">
  <%- include('./includes/loader.ejs') %>
  <div x-data="{ cartOpen: false , isOpen: false }">
    <%- include('./includes/navbar.ejs') %>
    <!--! ---------------------PRODUCTS SECTION--------------------------- -->
    <% if(products.length){ %>
    <main class="my-8">
      <div class="mx-auto px-6">
        <div class="mb-20">
          <div class="swiper-container">
            <div class="swiper-wrapper">
              <%- include('./includes/swiper-products.ejs') %>
            </div>
            <!-- Add Arrows -->
            <div
              class="swiper-button-next dark:text-red-500 bg-white dark:hover:bg-red-500 transition dark:hover:text-white rounded"
            ></div>
            <div
              class="swiper-button-prev dark:text-red-500 bg-white dark:hover:bg-red-500 transition dark:hover:text-white rounded"
            ></div>
          </div>
        </div>
        <div>
          <div class="swiper-container">
            <div class="swiper-wrapper">
              <%- include('./includes/swiper-products.ejs') %>
            </div>
            <!-- Add Arrows -->
            <div
              class="swiper-button-next dark:text-red-500 bg-white dark:hover:bg-red-500 transition dark:hover:text-white rounded"
            ></div>
            <div
              class="swiper-button-prev dark:text-red-500 bg-white dark:hover:bg-red-500 transition dark:hover:text-white rounded"
            ></div>
          </div>
        </div>
      </div>
    </main>
    <% } else {%>
    <h1
      class="text-center my-16 text-gray-200 text-4xl"
      style="font-family: rajdhaniSemibold"
    >
      Sorry, Not exist products for now...
    </h1>
    <% } %>
    <!--! ----------------------FOOTER------------------------ -->
    <%- include('./includes/footer.ejs') %>
  </div>
</body>
